import React from 'react';
import { Text } from 'react-native';
import AppLayout from '../components/AppLayout';

export default function NewsScreen() {
    return (
        <AppLayout title="news">
            <Text>News page content</Text>
        </AppLayout>
    );
}